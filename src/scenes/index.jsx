import { Typography, Box, useTheme } from "@mui/material";

const Login = () => {
    return (
        <Box>
            <Typography
                variant="h5"
                fontWeight="bold"
                sx={{ m: "0 0 5px 0" }}
            >
                Login
            </Typography>
        </Box>
    );
}

export default Login;